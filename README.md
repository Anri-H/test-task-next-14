## Test Task

First, check node version > 20:

```bash
node -v
```

Install packages:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Credentials:

```bash
username: test1
password: password
```
